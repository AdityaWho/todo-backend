services:
  - type: web
    name: todo-backend
    env: node
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm start
    # Environment variables will be set in Render Dashboard (not here for security)
